{
  "name": "cluster-capacity-planner",
  "version": "1.0.0",
  "private": true,
  "description": "What-if capacity sizing for Kubernetes workloads — builds to a single self-contained HTML file",
  "scripts": {
    "build": "node build.js",
    "test": "node calc.test.js"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-react": "^7.26.0"
  }
}
